# عمارة المشروع - Codexx Portfolio Platform

## نظرة عامة
منصة محافظ احترافية مبنية بـ Flask توفر:
- واجهة أمامية جميلة لعرض المحافظ
- لوحة تحكم إدارية متقدمة
- نظام إدارة المشاريع والعملاء والمهارات
- تحليلات الزوار ومتابعة الرسائل
- نظام المواضيع المتعددة

## البنية التكنولوجية

### الطبقة الخلفية (Backend)
- **الإطار العمل**: Flask 3.1.1
- **خادم الإنتاج**: Gunicorn 23.0.0
- **إصدار Python**: 3.11+

### قاعدة البيانات
- **النظام**: JSON (الحالي) / SQLAlchemy مع PostgreSQL (المعد للمستقبل)
- **النماذج المعرفة**:
  - `Workspace` - مساحة عمل أو محفظة
  - `User` - حسابات المستخدمين برموز الأدوار
  - `PortfolioSettings` - إعدادات المحفظة والموضوع
  - `Project` - المشاريع والأعمال
  - `Client` - إدارة العملاء
  - `Message` - الرسائل والتواصل
  - `Skill` - المهارات والخبرات
  - `VisitorLog` - تتبع الزوار
  - `AuditLog` - سجلات التدقيق الأمني

### الطبقة الأمامية (Frontend)
- **إطار العمل**: Bootstrap 5
- **JavaScript**: Vanilla JS + Alpine.js (اختياري)
- **المحرك**: Jinja2 templates

### الملفات الرئيسية

```
.
├── app.py (2,151 سطر)
│   ├── 53 مسار (Route)
│   ├── 95 دالة مخصصة
│   ├── نظام المصادقة والتفويض
│   ├── إدارة الملفات والرفع
│   ├── إدارة البيانات (JSON)
│   └── جدولة المهام (APScheduler)
│
├── models.py (254 سطر)
│   └── نماذج SQLAlchemy مستعدة للإنتاج
│
├── config.py
│   └── إعدادات التطوير والإنتاج
│
├── templates/ (34 ملف HTML)
│   ├── index.html - الصفحة الرئيسية
│   ├── dashboard/ - لوحة التحكم الإدارية
│   ├── error pages - صفحات الأخطاء
│   └── pages أخرى مخصصة
│
├── static/
│   ├── css/ - الأنماط (مع 6 موضوعات مختلفة)
│   ├── js/ - JavaScript الوظيفي
│   ├── themes/ (6 ملفات)
│   │   ├── luxury-gold.css ⭐ الافتراضي
│   │   ├── modern-dark.css
│   │   ├── clean-light.css
│   │   ├── silver-grey.css
│   │   ├── terracotta-red.css
│   │   └── vibrant-green.css
│   └── assets/
│       ├── uploads/ - الملفات المرفوعة
│       └── logos والصور
│
├── requirements.txt - المكتبات الأساسية
├── pyproject.toml - إعدادات المشروع
├── Procfile - إعدادات الإنتاج
├── render.yaml - تكوين النشر
└── data.json - قاعدة البيانات الحالية
```

## المسارات الرئيسية (Routes)

### المسارات العامة
- `/` - الصفحة الرئيسية
- `/portfolio/<username>` - محفظة المستخدم العامة
- `/register` - التسجيل
- `/upgrade` - صفحة التسعير
- `/payment` - معالجة الدفع
- `/catalog` - كتالوج المشاريع العام

### مسارات لوحة التحكم (مع الحماية)
- `/dashboard` - الرئيسية
- `/dashboard/general` - معلومات الملف الشخصي
- `/dashboard/about` - قسم حول
- `/dashboard/skills` - إدارة المهارات
- `/dashboard/projects` - إدارة المشاريع
- `/dashboard/clients` - إدارة العملاء
- `/dashboard/contact` - معلومات الاتصال
- `/dashboard/social` - روابط وسائل التواصل
- `/dashboard/messages` - الرسائل الواردة
- `/dashboard/settings` - الإعدادات
- `/dashboard/users` - إدارة المستخدمين (أدمن فقط)

### المسارات الإضافية
- `/cv-preview` - معاينة السيرة الذاتية
- `/download-cv` - تنزيل PDF

## نظام الأمان

### المصادقة والتفويض
- ✅ تجزئة كلمات المرور مع Werkzeug
- ✅ جلسات آمنة مع HTTP-only cookies
- ✅ أدوار المستخدمين (Admin, Demo, User)
- ✅ حماية ضد الهجمات

### الميزات الأمنية الأخرى
- ✅ التحقق من الملفات المرفوعة
- ✅ حماية XSS من خلال Jinja2
- ✅ حماية CSRF من خلال Flask
- ✅ تسجيل النشاط الأمني
- ✅ تحديد معدل الطلبات (Rate Limiting)
- ✅ رؤوس أمان (Security Headers)

## نظام البيانات الحالي

### الخزن: JSON
```json
{
  "users": [...],
  "portfolios": {
    "username": { ... }
  },
  "projects": [...],
  "clients": [...]
}
```

### المميزات:
- ✅ سهل التطوير والاختبار
- ✅ سريع للمشاريع الصغيرة
- ✅ لا يحتاج إعدادات قاعدة بيانات

### القيود:
- ❌ غير قابل للتوسع للآلاف من المستخدمين
- ❌ لا توجد معاملات ذرية
- ❌ صعوبة في الاستعلامات المعقدة

## المميزات الحالية

### 🎨 التصميم والمظهر
- 6 موضوعات احترافية قابلة للتبديل
- تصميم مستجيب (Responsive Design)
- رسوم متحركة ناعمة

### 👤 إدارة الملف الشخصي
- صورة الملف الشخصي
- المعلومات الأساسية
- قسم "حول"
- روابط وسائل التواصل

### 🛠️ إدارة المهارات
- إضافة/حذف/تحديث المهارات
- مستويات الكفاءة
- عرض بصري (شرائط التقدم)

### 📁 إدارة المشاريع
- CRUD كامل للمشاريع
- رفع الصور
- روابط GitHub و Demo
- علامات التقنيات

### 👥 إدارة العملاء
- تتبع المشاريع
- المواعيد النهائية
- تتبع الإيرادات
- حالة المشروع

### 💬 إدارة الرسائل
- استقبال الرسائل من نموذج الاتصال
- علامات قراءة/عدم القراءة
- حذف الرسائل
- تنبيهات Telegram

### 📊 التحليلات
- عد الزوار الفريدين
- الزيارات اليومية
- سجل النشاط

### 📄 توليد السيرة الذاتية
- معاينة PDF
- تنزيل PDF باستخدام WeasyPrint

### ⚙️ الإعدادات المتقدمة
- تبديل الموضوعات
- إعدادات SMTP (البريد)
- إعدادات Telegram
- النسخ الاحتياطية التلقائية
- إعادة تعيين بيانات Demo

## الجدولة والمهام الخلفية

### استخدام APScheduler
```python
- النسخ الاحتياطية كل ساعة
- إعادة تعيين بيانات Demo كل ساعة
- يحتفظ بآخر 20 نسخة احتياطية
```

## المتطلبات الحالية

```
Flask==3.1.1
Werkzeug==3.1.3
gunicorn==23.0.0
email-validator==2.2.0
flask-login==0.6.3
flask-sqlalchemy==3.1.1
flask-dance==7.1.0
psycopg2-binary==2.9.10
oauthlib==3.2.2
PyJWT==2.10.1
weasyprint==65.1
APScheduler==3.10.4
requests==2.31.0
alembic==1.13.0
sqlalchemy==2.0.23
```

## نقاط الضعف الحالية

1. **قاعدة البيانات**: JSON غير قابل للتوسع
2. **المصادقة**: لا توجد OAuth (بيانات الاعتماد المشفرة مطبوعة في الكود)
3. **البحث**: لا يوجد نظام بحث متقدم
4. **الأداء**: لا توجد ذاكرة تخزين مؤقت (Caching)
5. **SEO**: محدود جداً
6. **الاختبار**: لا توجد اختبارات آلية
7. **المراقبة**: لا توجد أنظمة تسجيل متقدمة

## الاتجاهات المستقبلية

انظر إلى ملف `ROADMAP_AND_RECOMMENDATIONS.md` للتفاصيل الكاملة.
